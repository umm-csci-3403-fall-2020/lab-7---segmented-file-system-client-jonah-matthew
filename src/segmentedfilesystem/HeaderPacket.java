package segmentedfilesystem;

public class HeaderPacket extends Packet {
    
    //Parses the packet data as a header packet. Has methods like getFileName()

    //1 status byte | 1 file ID byte | rest of bytes are file name

}
